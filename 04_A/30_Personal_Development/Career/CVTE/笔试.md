# 不定项选择题

1. 下列哪个命令可用于获取 shell 命令的使用说明？
	- `more`
	- `help`
	- `pwd`
	- `man`
	- `more` 用于分页查看文件内容 `help` 并非标准 shell 命令 ==（D）==

2. 关于差分信号，下列哪些说法是正确的？
	- 可有效降低电磁干扰（EMI）
	- 便于识别微弱信号
	- 具备较强的抗干扰能力
	- 曼切斯特编码属于差分信号的一种
	- 差分信号使用两根信号线传输相反相位的信号，产生的电磁场相互抵消，从而减少电磁辐射和干扰。差分信号通过抵消共模噪声，能提高信噪比，使接收器更容易检测和识别微弱信号，尤其在噪声环境中。差分信号对共模噪声（如电源噪声或环境干扰）有很强的免疫力，因为噪声同时影响两根信号线，在差值计算中被消除。曼切斯特编码是一种数字编码方案，用于同步时钟和数据，但它本身不是差分信号。差分信号是一种物理层传输方式，而曼切斯特编码可以在差分或单端信号上使用。==（ABC）==

3. 以下关于 Linux 命令的描述，哪些是错误的？
	- `rm` 命令可直接删除目录
	- `comm` 命令可用于比较两个文本文件中的相同内容
	- `df` 命令可用于查看当前目录占用的磁盘空间大小
	- `ln -s a.txt b.txt` 命令创建的符号链接名称为 `a.txt`
	- `rm` 默认仅删除文件，若要删除目录需使用 `-r` 选项，因此 A 选项错误； `comm` 用于对比两个已排序文件的内容，B 选项正确； `df` 用于查看整个文件系统的磁盘使用情况，而非单个目录，C 选项错误； `ln -s a.txt b.txt` 实际作用是创建 `b.txt` 作为 `a.txt` 的符号链接，D 选项错误  ==（ACD）==

4. 嵌入式系统中最常采用的数据传输方式是？
	- 查询方式
	- I/O 处理机
	- 直接存储器访问（DMA）
	- 中断方式
	- ==（C）==

5. 下列哪种方式可以正确地进行字符串赋值？
	- `char *sp; *sp="Dream future!";`
	- `char s[20]; s="Dream future!";`
	- `char *sp="Dream future!";`
	- `char s[20];*s="Dream future!";`
	- 编译错误：无效的类型转换（从 'const char*' 到 'char'）；数组名不能被赋值；正确的赋值方式；编译错误：无效的类型转换（从 'const char*' 到 'char'）。==（C）==

6. 关于 C 语言，以下哪个说法是正确的？
	- `int* p1;` 可以这样赋值 `*p1=5;`
	- "1" 占 1 个字节
	- 1 bit = 8 byte
	- `printf` 中使用格式符 `"%5s"` ，如果输出的字符串长度大于 5，则按原字符串长度输出
	- `int* p1;` 只是声明了指针变量，但未初始化，直接赋值会导致未定义行为；`"1"` 不是字符 `'1'`，而是字符串 `"1\0"`，因此占用两个字节；1 字节（byte）等于 8 位（bit），而不是 8 字节；`"%5s"` 指定最小宽度为 5，但不会截断超出长度的字符串；==（D）==

7. 下列哪个命令不添加参数的情况下可以看到文件大小 
	- `df`
	- `du`
	- `free`
	- `ls`
	- ==AB==

8. `char x=0xA5;` 则 `(2+x)^(~3)` 的值二进制表示为： 
	- 10100111
	- 11111101
	- 10101001
	- 01011011
	- ==D==

9. 
	```c
	struct cvte1{
		char str;
	    short s;
	    int num;
	}
	
	struct cvte2{
		char str;
	    int s;
	    short x;
	}
	```
	- `sizeof(cvte2)=8`
	- `sizeof(cvte1)=7`
	- `sizeof(cvte2)=12`
	- `sizeof(cvte1)=8`
	- ==CD==

10. 属于块设备的有哪些？
	- 硬盘
	- 光盘（字符设备）
	- 串口设备（字符设备）
	- 终端（字符设备）
	- 块设备 (Block Device)，以固定大小的数据块为单位进行读写操作，硬盘（包括HDD和SSD），U盘、SD卡，光盘驱动器（CD/DVD/Blu-ray），磁盘阵列；字符设备 (Character Device)以字符流为单位进行顺序读写，串口设备，终端，键盘、鼠标，打印机。==A==

11. 一个文件的权限为 `lrwxr-xr-x 1 root shell`, 下面哪些说法是正确的？
	- root用户可以删除该文件
	-  其他用户可以删除该文件
	-  这是一个目录文件（符号链接）
	-  shell用户可以删除该文件
	- 第一个字符 `l` 表示这是一个符号链接文件，而不是目录文件。==A==

12. 下面关于面向对象的一些理解哪些是正确的？
	- 面向对象的最重要特性是继承、封装和多态
	- C是静态类型的面向过程编程语言，C++是动态类型的面向对象编程语言
	- 面向对象语言强调抽象和封装，提高可维护性，而面向过程的编程语言性能更好
	- 函数式的语言是面向对象的语言
	- C语言是静态类型的面向过程编程语言，但C++也是静态类型的语言，尽管它支持面向对象编程，但类型检查在编译时进行，而非运行时动态类型。函数式语言强调不可变数据和纯函数，而面向对象语言强调对象和状态封装。==AC==

13. 软件有哪些方式接收串口数据？
	- 同步
	- 查询
	- 缓冲
	- 中断
	- ==ABCD==

14. 下面哪些命令可以用来停止系统中的进程？
	- `shutdown`
	- `stop`
	- `down`
	- `kill`
	- ==ABD==

15. 
	```c
	int main(void){
		unsigned char c=0xfc;
		unsigned int i=~c ;
		int a=1;
		int b;
		b=sizeof( a)++;
		printf("0x%x , %d , %d \n" ,i,a,b);
		return 0;
	}
	```
	- 0xffffff03,2,1
	- 0x00000003,2,4
	- 0x00000003,1,5
	- 0xffffff03,1,4
	- ==D==

16. 若进栈序列为1，2，3，4，进栈过程中可以出栈，则下列不可能的一个出栈序列是？
	-  2，3，4，1
	-  1，4，3，2
	-  3，1，4，2
	-  3，4，2，1
	- ==C==

17. crontab文件中，`5 7 ** 3 /usr/bash /usr/local/run.sh` 中命令的执行时间为（）
	- 周五7:03
	- 周五3:07
	- 每周三7:05
	- 每周三5:07
	- ==C==

18. 关于嵌入式系统中的中断函数，描述正确的是（）
	-  中断函数不可以传递形参
	-  中断函数不可以有返回值
	-  中断函数可以传递形参
	-  中断函数可以有返回值
	- 中断函数是由硬件中断触发的，不是由程序代码直接调用的，因此无法传递参数。中断函数没有调用者来接收返回值，因此不能有返回值。==AB==

19. 下面哪些方法可以用于控制单个LED发光二极管的亮度?
	-  单片机通过ADC电路输出得到一个模拟电压
	-  单片机输出PWM信号，通过积分电路调节LED二极管的亮度
	-  单片机输入PWM信号，通过放大电路调节LED二极管的亮度
	-  单片机通过DAC电路输出得到一个模拟电压
	- ADC（模数转换器）用于将模拟信号转换为数字信号，是输入设备，无法直接输出模拟电压，因此，该方法不能用于控制LED亮度。PWM（脉冲宽度调制）信号通过改变占空比来控制平均电流，积分电路可以将PWM信号转换为平滑的模拟电压，从而调节LED亮度，这是常用方法。片机输入PWM信号意味着它在接收信号，而不是主动控制LED，放大电路可能需要其他控制信号，但该方法不直接由单片机输出控制，因此不适用于调节LED亮度。DAC（数模转换器）可以将数字信号转换为模拟电压，通过调节输出电压来控制LED的电流，从而调节亮度，但需注意，LED是电流驱动器件，通常需要串联电阻或电流源来确保稳定驱动。==BD==

20. 使用GCC编译器，在32位平台中，有如下定义的结构体，请问sizeof(struct Astruct)为多少:
	```c
	struct Astruct {
		 char a;
		 char b;
		 int c;
	 }_attribute__((packed));
	```
	- 7
	- 8
	- 6
	- 5
	- 在GCC编译器中，使用 `__attribute__((packed))` 属性会取消结构体的字节对齐，使所有成员紧挨着存放，没有填充字节。==C==

21. 以下关于I/O口说法正确的是（）
	- I/O口可以通过软件设置为高阻态
	- I/O口可以通过硬件设置为上拉状态
	- I/O口可以通过硬件设置为高阻态
	- I/O口可以通过软件设置为上拉状态
	- ==ABCD==

22.  已知字母A的ASCII码值为65，字母a的ASClI码值为97，使用默认的gcc参数编译生成以下程序

	```c
	main()
	{
		char a = 'A';
		int b = 20;
		printf("%d, %o",(a=a*a,a*b,b),a+'a'-'A',b);
	}
	```
	- 输出结果为20,141
	- 表达式非法，输出零或不确定值
	- 因输出项过多，无输出或输出不确定值
	- 输出结果为20,141,20
	- ==A==

23. 引入多道程序技术以后，处理器的利用率()
	- 降低了
	- 没有变化，只是程序的执行方便了
	- 大大提高
	- 没有影响
	- 多道程序技术的引入确实显著提高了处理器的利用率。这是因为:1. 在单道程序系统中,当程序进行I/O操作时CPU只能等待,造成大量空闲时间被浪费。而多道程序允许在一个程序等待I/O时,CPU可以转去执行其他程序的计算任务。2. 多道程序技术实现了CPU和多个程序之间的交替执行,有效地利用了原本被浪费的CPU时间片段,从而提高了CPU的使用效率。3. 通过合理的调度算法,多道程序可以让CPU始终保持"忙碌"状态,大大提升了资源利用率。==C==

24. 以下说法正确的是()
	- 在计算机中，运算器访问寄存器的速度是最快的
	- 在等待中断的过程中，CPU不可以处理其他任务
	- 同一段代码每次运行的时间一定是一样的
	- 软中断是由软件触发的一种中断
	- ==AB==

25. 以下说法正确的有()
	- 在时间片轮询调度算法中，时间片越短则CPU利用率越高
	- 优先级越高的进程占用CPU的运行时间就一定越多
	- 在遍历大型二维数组`int a[x][y]`时，先遍历x或先遍历y的处理时间都是一样的
	- 使用cache可以提高CPU的利用率
	- 时间片太短会导致进程切换过于频繁,增加系统开销,反而会降低CPU利用率。Cache通过缓存最常用的数据,减少CPU访问主存的次数,能显著提高CPU的利用率。==D==

26. 具有1000个节点的二叉树的最小深度为（）(第一层深度为1）
	- 11
	- 12
	- 9
	- 10
	- ==D==

27. 以下程序统计给定输入中每个大写字母的出现次数(不需要检查输入合法性)

	```c
	void AlphabetCounting(char a[],int n){
		int count[26] = {}, i, kind = 10;
		for(i = 0;i < n;++i) (1);
		for(i = 0;i < 26;++i){
			if(++kind > 1) putchar(';');
			printf("%c=%d", (2));
		}
	}
	```
	- `++count[a[i]-'Z'];'Z'-i,count['Z'-i]`
	- `++count['A'-a[i]];'A'+i,count[i]`
	- `++count[i];i,count[i]`
	- `++count['Z'-a[i]];'Z'-i,count[i]`
	- `++count[a[i]];'A'+i,count[a[i]]`
	- ==D==

28. 设非空二叉树中度数为0的结点数为n0，度数为1的结点数为n1,度数为2的结点数为n2,则下列等式成立的是（）
	- n0=n1+n2
	- n0=2n1+1
	- n0=n2+1
	- n0=n1+1
	- 1) 二叉树的第i 层上至多有2^(i-1) 个结点。 2) 深度为k 的二叉树至多有2^k-1 个结点。 满二叉树：深度为k，有2^k-1 个结点。 完全二叉树：给满二叉树的结点编号，从上至下，从左至右，n 个结点的完全二叉树中结点在对应满二叉树中的编号正好是从1 到n。 3) 叶子结点n0，度为2 的结点为n2，则n0 = n2+1。 考虑结点个数：n = n0 + n1 + n2 考虑分支个数：n-1 = 2n2 + n1 可得n0 = n2+1 4) n 个结点的完全二叉树深度为。log2(n+1) 5) n 个结点的完全二叉树，结点按层次编号 有： i 的双亲是n / 2，如果 i = 1 时为根（无双亲）； i 的左孩子是2i，如果2i>n，则无左孩子； i 的右孩子是2i + 1，如果2i + 1>n 则无右孩子。==C==

29. IP地址205.140.36.68的哪一部分表示网络号()
	- 205
	- 205.140
	- 68
	- 205.140.36
	- 在IP地址205.140.36.68中,205.140.36是网络号部分,而68是主机号部分。这是因为:  这是一个C类IP地址(第一段在192-223之间),按照IP地址分类规则:  C类地址的前24位(前三段)为网络号，最后8位(最后一段)为主机号。因此205.140.36整体构成了网络号==D==

30. 一棵深度为4的三叉树，最多有多少个节点（）
	- 24
	- 40
	- 36
	- 54
	- ==B==

31.   有两个袋子，白色袋子里有7个红球和3个蓝球，黑色袋子里有3个红球和7个蓝球。每次取一个球，取完立刻放回，所有球都从某一个袋子里取，袋子的选择是随机的。共取出6个红球和4个蓝球。问所有球是从黑色袋子里取出的概率是（）
	- 0.1
	- 0.48
	- 0.66
	- 0.16
	- ==D==

32. 关于二叉树，下面说法正确的是（）
	- 二叉树中至少有一个节点的度为2
	- 一个具有1025个节点的二叉树，其高度范围在11到1025之间
	- 对于n个节点的二叉树，其高度为 $n*log(n)$
	- 二叉树的先序遍历是EFHIGJK,中序遍历为HFIEJKG,该二叉树的右子树的根为G
	- 二叉树的节点度数可以为0、1或2,并不要求必须至少有一个节点的度为2。对于一个有1025个节点的二叉树,其高度范围确实在11到1025之间。对于n个节点的二叉树,其高度h的范围是：最小高度⌈log₂(n+1)⌉；最大高度为n。==BD==

33. 以下说法正确的有()
	- 多个进程操作同一个文件时，应该要考虑到文件的一致性问题
	- 可通过文件在不同进程间进行数据传递和共享
	- 可以通过全局变量在不同进程间传递数据
	- 一个进程可以访问到所有物理内存空间
	- 因为每个进程都有自己独立的地址空间，进程中的全局变量只在该进程内部可见，其他进程无法直接访问。要实现进程间数据传递需要使用专门的IPC(进程间通信)机制。==AB==

34. 以下选项中采用分治方法的算法有（）
	- 堆排序算法
	- 插入排序算法
	- 归并排序算法
	- 二分查找算法
	- 快速排序算法
	- ==CDE==

35. 下面算法中可以判断出一个有向图是否有环的是：（）
	- 求最短路径
	- 深度优先遍历
	- 广度优先遍历
	- 拓扑排序
	- ==BD==

36. IP 数据报分片的重组通常发生在（）
	- 源主机和数据经过的路由器上
	- 源主机上
	- 数据报经过的路由器上
	- 目的主机上
	- ==D==

37. 叉树的先序遍历序列和后序遍历序列正好相反,则该二叉树满足的条件是
	- 空或只有一个结点
	- 高度等于其结点数
	- 该二叉树是完全二叉树
	- 所有结点无右孩子
	- ==B==



# 编程题

## 2.1

给定一个字符串，将字符串中的每个单词逐个翻转

示例：

1. 输入：“I Am a Programmer!"
2. 结果：“I mA a remmargorP!
3. 函数形式如下：`char* ReverseString(char* Source)`

解答：

1. 以空格分割每个单词
2. 创建函数用于反转每个单词
3. 增加判断是否为字母的函数，确保句子最后的标点符号不会被翻转

## 2.2

给定一个正整数 n ，你可以做如下操作：

- 如果 n 是偶数，则用 n/2 替代 n ；
- 如果 n 是奇数，则可以用 n+1 或 n-1 替换 n ；
- 返回 n 变为 1 所需的最小替换次数；

示例：

- 输入：n=8
- 输出：3
- 解释：8 -> 4 -> 2 -> 1

## 2.3

给定一个MAC地址，判断该MAC地址是否在某给定的MAC地址段中，是输出TRUE，否输出FALSE

举例：

输入1: B4:AD:A3:31:E2:44, B4:AB:A3:31:E2:44-B4:AE:A3:31:E2:44
输出:TRUE

输入2:B4:AD:A3:31:E2:44, B4:AD:A3:31:E2:46-B4:AD:A3:31:E2:4A
输出:FALSE  

## 2.4

给定一个乱序整形数组，找出其中数字连续的最长序列，如果出现长度相同的情况，返回第一个即可。

示例1: 输入: `[0，15,7，13，16,17，2，4，5]`
输出: `[15,16,17]`

示例2: 输入︰ `[0，15,1,7，13,16,17,2,4,5]`
输出: `[0,1,2]`

示例3: 输入: `[0，15,8,11，13，19,17,2,21,5]`
输出:`[0]`

