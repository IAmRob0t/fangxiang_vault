- `Walk`: 与 `Walk` 有冲突的日程请进行调整，依照往后延的顺序，如果实在调整不了没有空余时间的话 `Flexible / Buffer` 的时间可以缩短。每周六的 `Walk` 可调整在 `Live Class: Project Practice` 之后，可适当缩短 `Post-Class Review` 的时间
- `Strength Training`: 解决 `Strength Training` 日程的冲突问题，可适当缩短当天的 `Deep Work: Session B`
- `Kindle Reading`: 解决 `Kindle Reading` 日程的冲突问题，可适当缩短 `Kindle Reading` 和 `Flexible / Buffer` 的时长
- `Monthly Review`: 解决 `Monthly Review`